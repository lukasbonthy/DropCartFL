import { index, integer, sqliteTable, text } from "drizzle-orm/sqlite-core";
export const bookings=sqliteTable("bookings",{id:text("id").primaryKey(),reference:text("reference").notNull().unique(),requestDigest:text("request_digest").notNull(),createdAt:integer("created_at").notNull(),arrivalAt:integer("arrival_at").notNull(),etaMinutes:integer("eta_minutes").notNull(),status:text("status").notNull().default("pending"),customerName:text("customer_name").notNull(),phone:text("phone").notNull(),address:text("address").notNull(),city:text("city").notNull(),state:text("state").notNull().default("FL"),zip:text("zip").notNull(),groceryLoad:text("grocery_load").notNull(),stairs:integer("stairs",{mode:"boolean"}).notNull(),notes:text("notes").notNull(),contactConsent:integer("contact_consent",{mode:"boolean"}).notNull(),sourceKey:text("source_key").notNull()},table=>[index("idx_bookings_source_created").on(table.sourceKey,table.createdAt)]);
